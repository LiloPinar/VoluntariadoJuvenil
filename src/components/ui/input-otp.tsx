import * as React from "react";

// Stubbed InputOTP exports (archived)

const InputOTP: React.FC = () => null;
const InputOTPGroup: React.FC<React.PropsWithChildren<Record<string, unknown>>> = ({ children }) => (
	<div>{children}</div>
);
const InputOTPSlot: React.FC = () => null;
const InputOTPSeparator: React.FC = () => null;

export { InputOTP, InputOTPGroup, InputOTPSlot, InputOTPSeparator };
